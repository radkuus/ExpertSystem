<UserControl x:Class="ExpertSystem.WPF.Controls.TopBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExpertSystem.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid 
             Grid.Row="0" 
             RenderOptions.EdgeMode="Aliased" 
             Background="Black">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" 
                    Background="Transparent" 
                    MouseLeftButtonDown="DragAreaMouse"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button  Content="—" 
                         Width="46"
                         Height="32" 
                         Click="MinimizeButton"
                         Style="{StaticResource WindowControlButton}"/>
                <Button  x:Name="MaximizeButtonn"
                         Content="▢" 
                         Width="46" 
                         Height="32" 
                         Click="MaximizeButton"
                         Style="{StaticResource WindowControlButton}"/>
                <Button  Content="✕"
                         Width="46" 
                         Height="32" 
                         Click="CloseButton"
                         Style="{StaticResource WindowControlButtonClose}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
